Create one short explanation as a single HTML fragment.

Goal:
- Explain the meanings, usage, contrast, form, or word formation requested in DESCRIPTION.
- Cover all focus units named in DESCRIPTION, and all TARGETS if TARGETS are provided.
- Split several distinct points into clear HTML sections inside the same fragment.

Rules:
- This exercise is explanation only.
- No questions to the learner, no tasks, no input fields.
- Keep each block compact and self-contained.
- Use learner-friendly examples.
- All text, except examples, must be written in the learner language. Examples must be in English
- Prefer concrete rules, contrasts, and short examples over abstract wording.
- If focus units are lexemes, explain meaning, difference, usage, and any requested word formation.
- If focus units are grammar, explain when to use it, the form, and 2-4 short examples.
- Do not invent extra topics beyond DESCRIPTION or provided TARGETS.

HTML rules:
- Output an HTML fragment only, not a full page.
- Do not write CSS, JavaScript, <html>, <body>, or code fences.
- Allowed tags only: <h2>, <h3>, <p>, <strong>, <em>, <ul>, <li>, <blockquote>.
- Use <h2> for the main idea, <h3> for subpoints, <p> for short explanations, <ul><li> for patterns/examples, <blockquote> for one short tip only if useful.

Output format:
Return plain text, not JSON.
Return only one complete HTML fragment.
Do not add separators, block markers, titles outside HTML, JSON, markdown fences, or escaped quotes.
